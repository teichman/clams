<package>
  <description brief="pcl_1_7">

    Shell package for PCL 1.7.

    This assumes you have openni installed.

  </description>
  <author>teichman</author>
  <license>BSD</license>
  <review status="unreviewed" notes=""/>
  <url>http://ros.org/wiki/pcl_1_7</url>

    <export>
      <!-- -isystem tells the compiler to not print warnings generated by these headers. -->
      <!-- -isystem flags show up in rospack cflags-only-other. -->
      <cpp cflags="-isystem/usr/include/ni -isystem/usr/include/openni $(for dir in `find ${prefix} -name include`; do echo -isystem${dir}; done)"
           lflags="-L${prefix}/release/lib -Wl,-rpath,${prefix}/release/lib `for name in ${prefix}/release/lib/*.so; do foo=${name#${prefix}/release/lib/lib}; echo -l${foo%.so}; done`" />
    </export>
  
</package>


